#include <iostream>
using namespace std;

int main() {
    int arr[5] = {15, 40, 10, 70, 59};
    int n = sizeof(arr) / sizeof(arr[0]);

    cout << "Array elements: ";
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }
    return 0;
}
